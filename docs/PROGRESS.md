# Progress

Single source of truth for "where are we". Read this first in every session. Git is the ground truth: if this file and `git log` disagree, believe git and fix this file.

## Current state
- Last completed task: 13 (T016 — Core seeders: roles, demo accounts, 37 settings, slots, tables)
- Next task: 14 (T022 Role middleware, policies, permissions matrix — FR04, 03.3)
- In progress: none
- Branch: develop
- Environment: Laravel 13.17 / PHP ^8.3, MySQL `coolaroo` database, `APP_TIMEZONE=Australia/Melbourne`, `APP_NAME="Coolaroo RMS"`. Auth guards (`customer`/`staff`) configured (T002). Reverb installed and wired: `BROADCAST_CONNECTION=reverb`, `REVERB_*`/`VITE_REVERB_*` set in `.env`/`.env.example`, `config/broadcasting.php` and `config/reverb.php` published, `resources/js/echo.js` created and imported from `resources/js/app.js`, `routes/channels.php` wired into `bootstrap/app.php`. `php artisan reverb:start`, `npm run build`, and `php artisan test` all confirmed working. `config/services.php` has `stripe` and `ai` blocks (T004) — `AI_BASE_URL`/`AI_MODEL` set, but the three real credentials (`STRIPE_KEY`, `STRIPE_SECRET`, `AI_API_KEY`) are still blank in `.env` and must be filled in locally before T070 (Stripe) or T120 (AI) can run against the live APIs. Four layouts (T005, converted to Blade components after T014): `<x-layouts.public|customer|staff|admin>` in `resources/views/components/layouts/`, all wrapping `<x-document>`, with chrome in `components/site/` and `components/dashboard/` — no `@extends`/`@yield` anywhere (SDD 8.7, AGENTS rule 11); local-only `/_preview/{public,customer,customer-table,staff,admin}` routes (`routes/web.php`, gated on `app()->isLocal()`) for eyeballing them. `public/css/style.css` and `public/css/dashboard.css` live (T006), plus `public/images/logo.svg`; `vite.config.js` now bundles only `resources/js/app.js` (no CSS entry, no font loader). No `/` route — `GET /` 404s until T030 builds the real homepage. `role`, `staff`, `customer`, `setting` tables migrated (T010); `restaurant_table`, `slot_capacity`, `reservation`, `visit` tables migrated (T011); `menu_category`, `menu_item`, `menu_item_size`, `add_on_group`, `add_on_option`, `allergen`, `menu_item_allergen`, `dietary_tag`, `menu_item_dietary_tag` tables migrated (T012); `orders`, `order_item`, `order_status_history`, `payment`, `refund`, `feedback`, `audit_log`, `historical_data_management` tables migrated (T013). All 25 design entities now exist. Local DB server is MariaDB 10.4.32 (XAMPP), driven through Laravel's `mysql` driver; 06.1 specifies MySQL 8 (see T013 log). 25 Eloquent models in `app/Models/` (T014). 11 backed enums in `app/Enums/` with transition maps (T015), cast on the models. Seeded: 4 roles, 5 demo accounts (`password` for all, 10.4), 37 settings (06.5), 9 reservation slots, 12 restaurant tables (T016) — via `php artisan migrate:fresh --seed`. `tests/Feature/` kept alive with a `.gitkeep` (see log) — Git doesn't track empty directories, and deleting `ExampleTest.php` in T006 had silently taken the directory itself out of the checkout.
- Blocked: none

## Open deviations from the SDD
- None. (The Tailwind `vite.config.js`/`package.json` scaffold was removed in T001; the last of it — `resources/css/app.css`'s leftover `@source` directives, and the `welcome.blade.php`/`/` route that depended on it — was removed in T006. See log.)

## Log
<!-- Newest first. One entry per task, one task at a time.
### YYYY-MM-DD — Task <n> (<Txxx>) — done — <tool>
- Added / changed: <files>
- Notes: <decisions, rule IDs applied>
- Deviation: <FR/BR ID + what changed + why>  (or: none)
- Follow-up: <what is deliberately left to a later task>
-->

### 2026-09-19 — Task 13 (T016) — done — Claude Code
- Added / changed: `database/seeders/{RoleSeeder,StaffSeeder,CustomerSeeder,SettingSeeder,SlotCapacitySeeder,RestaurantTableSeeder}.php` (new), `database/seeders/DatabaseSeeder.php` (replaces T003's placeholder comment), `docs/PROGRESS.md`
- Notes: Six seeders per table, called from `DatabaseSeeder::call()` in FK-safe order (`RoleSeeder` before `StaffSeeder`). Deliberately not named `DemoSeeder` — 09.2 reserves that exact class name for the Selenium data source, which is task T017 ("Demo seeder at realistic volume: menu, orders, reservations, feedback"), not this one; T016's own title says "demo accounts", a much smaller thing than T017's "demo data". Demo account password confirmed with the developer before writing any code: `password` for all 5 accounts (10.4), passed as a plain string to `password_hash` and hashed automatically by the model's `'hashed'` cast (T014) rather than `Hash::make()`. `role.landing_screen`: 3 of 4 values are 06.4.1's own examples (`admin.dashboard`, `floor.index`, `kds.kitchen`); `kds.bar` isn't given directly but is the existing `/staff/kds/bar` route path already sitting in T005's `components/dashboard/nav-staff.blade.php`, so it's a direct extension of the stated pattern, not a new decision. No named routes exist yet for any of these — they're just strings until T021/T022. All 37 settings seeded verbatim from 06.5, in the dictionary's own row order; `setting_value` stored as the Default column's string as-is (06.4.4: cast by `value_type` at read time, not by the seeder). `venue_address` keeps 06.5's literal "xxx Sydney Road" placeholder rather than silently fixing it. 12 tables (3 sections: Dining ×8, Bar ×2, Outdoor ×2) and 9 hourly reservation slots (12:00–20:00, higher `max_covers` in the 18:00–20:00 dinner window) — 06.4.5/06.4.6 give column shapes, not a floor plan or schedule, so both are explicitly flagged as adjustable placeholders, not SDD numbers. `RestaurantTableSeeder` doesn't set `status`; it's guarded on the model (T014) and the migration already defaults it to `available`. Demo customer's `email_verified_at` is pre-set to `now()`, not left null, so the one seeded customer account can demonstrate UC13 (reservations) without an extra manual verification step first — BR42 would otherwise block it. Verified after `migrate:fresh --seed`: row counts are exactly 4/4/1/37/9/12; all 5 demo accounts authenticate through their real guard (`Auth::guard('staff'|'customer')->once([...])`) with the confirmed password; each role's `landing_screen` matches the intended value; the demo customer's `email_verified_at` is non-null; a few settings spot-checked (`qr_stock_buffer_multiplier`, `qr_ordering_enabled`, `venue_name`) round-trip as plain strings, as expected until `SettingService` exists. `php artisan test` green (21 passed, unaffected by this task).
- Deviation: none
- Follow-up: `SettingService` (07.4) still doesn't exist, so every setting reads back as a string (`"5"`, `"1"`) rather than cast by `value_type` — expected, not a bug. First login-redirect task (T021/T022) must use the exact `landing_screen` strings seeded here when naming those routes. T017 builds the real `DemoSeeder` (menu, orders, reservations, feedback) that 09.2's Selenium note refers to — this task's seeders are not it. If a specific floor plan or slot schedule is wanted instead of this guide's placeholders, `RestaurantTableSeeder`/`SlotCapacitySeeder` are the two to revisit.

### 2026-09-19 — Layouts to Blade components (between T014 and T015, requested by developer) — done — Claude Code
- Added / changed: `resources/views/components/document.blade.php` (new), `components/layouts/{public,customer,staff,admin}.blade.php` (new, replace `layouts/*.blade.php`), `components/site/{header,footer,order-bar}.blade.php` and `components/dashboard/{topbar,footer,nav-item,nav-admin,nav-staff}.blade.php` (moved with `git mv` from `layouts/partials/`), `resources/views/preview/*.blade.php`, `docs/sdd/07-architecture.md` (7.3), `docs/sdd/08-ui-design.md` (8.1, 8.7), `AGENTS.md` (rule 11), `docs/PROGRESS.md`
- Removed: `resources/views/layouts/` (the four `@extends` layouts and `partials/head.blade.php`)
- Notes: Pages now use `<x-layouts.admin title="…" page-title="…">…</x-layouts.admin>` instead of `@extends`/`@section`. The old `partials/head` opened `<html><head>` and each layout closed `</body></html>`, and chrome partials read the page's `@yield` values. Components can't do either, so `<x-document>` now owns the whole HTML document (doctype, head, body, `@stack('scripts')`). The four layouts wrap it and pass values down as `@props`: `title`, `description`, `table-label`, `page-title`, `page-sub`, `body-class`. Defaults that used to live in `@yield(..., default)` moved to `??` inside the component (description, `Coolaroo` page title, today's date as sub-title), so a layout passing `null` still gets the default. `@push('scripts')`/`@push('widgets')` kept, because stacks can be filled from nested components (e.g. the future chat widget) and named slots can't. Verified by rendering all five `/_preview/*` views before and after the change and diffing the output with whitespace normalised. Markup is identical except for two things: the preview pages' own wording, and staff's empty `<body class="">` becoming `<body>`. Also checked with `Blade::render` that `body-class`, `page-title`/`page-sub`, `@push('scripts')` (lands before `</body>`), `@push('widgets')` (between footer and back-to-top) and the no-title `<title>` fallback all work. Nothing under `resources/views` uses `@extends`/`@yield`/`@section`/`@include` any more. `php artisan test` green (1 passed).
- SDD changes (not deviations, docs and code changed together): 7.3's views tree now lists `components/`. 8.1's ten `partials/…` entries (S03, S15, S17, S18, S23, S25, S26, S28, S29, S31) are now component tags (`<x-menu.item-modal>`, `<x-floor.table-drawer>`, …). 8.7 gains Layouts, Components and Page scripts rows.
- Follow-up: T030 and every later screen task builds pages on `<x-layouts.*>` and its fragments as components, per 8.7. `notes/T005-guide.md` still describes the old `@extends` layouts. It's historical and local-only, so it was left alone.

### 2026-09-19 — Task 12 (T015) — done — Claude Code
- Added / changed: `app/Enums/{OrderStatus,PaymentStatus,OrderItemStatus,PaymentAttemptStatus,RefundStatus,TableStatus,ReservationStatus,VisitCloseReason,PaymentMethod,RefundMethod,Destination}.php`, `app/Enums/Concerns/HasTransitions.php`, `app/Exceptions/InvalidTransitionException.php`, `app/Models/{Order,OrderItem,MenuItem,Payment,Refund,RestaurantTable,Visit,Reservation}.php`, `tests/Unit/Enums/StateMachineTransitionsTest.php`, `docs/PROGRESS.md`
- Notes: 11 backed enums per 07.3's list — 7 are state machines (05.1–05.6, 05.8) with a `transitions(): array` method, 4 are plain value lists with no transitions (`VisitCloseReason`, `PaymentMethod`, `RefundMethod`, `Destination`). 5.7 (visit lifecycle) has no enum, since it's derived from `opened_at`/`closed_at`, not a status column — matches 07.3, which doesn't list one. All 7 state enums share `canTransitionTo()`/`ensureCanTransitionTo()` through a `HasTransitions` trait rather than repeating them; each enum only writes its own `transitions()` map, copied case-by-case from 05. Every `transitions()` is an exhaustive `match ($this)` — PHP throws `UnhandledMatchError` if a case is ever added without updating the map, so a missing entry fails loudly instead of silently allowing nothing. `InvalidTransitionException` extends Symfony's `HttpException` with code 409 per 07.11; nothing calls it yet, and the flash-message half of 07.11 is left for whichever task first drives a transition from a web form. Three things from 05 deliberately stayed out of the enums, per hard rule 2 (business logic lives in Services): guards (e.g. "no succeeded payment" before `pending_payment → cancelled`), side effects (timestamps, `order_status_history`, `audit_log`, broadcasts), and 5.6's admin "any → any with reason" table override — putting the override in the map would make every transition legal and defeat the point of having one; it needs its own service method instead. Added enum casts to the 8 models with an enum-backed column (`Order` ×2, `OrderItem` ×2, `MenuItem`, `Payment` ×2, `Refund` ×2, `RestaurantTable`, `Visit`, `Reservation`) — only the `casts()` array and a `use` import changed on each, nothing else. Status columns were already in `$guarded` since T014, unaffected by this task. Tests are PHPUnit, not Pest — `composer.json` only has `phpunit/phpunit`, the existing test is PHPUnit, and 09-test-design allows either; adding Pest would be a separate dependency decision. One data-provider-driven test file checks, for every state machine, all N×N from/to pairs against a map typed independently from the enum code (36 pairs for orders, 64 for reservations, etc.), plus every case's value against 06.3, plus the 4 plain enums' values, plus that an illegal transition throws a 409 with the right message and a legal one doesn't throw. 21 tests, 206 assertions, all green — the exact count the guide predicted. Also verified outside the test suite: `Order::make()->forceFill(['status' => 'paid'])->status->name` returns `Paid` (cast round-trips); `OrderStatus::Paid->canTransitionTo(OrderStatus::Served)` is `false` and `ReservationStatus::Confirmed->canTransitionTo(ReservationStatus::Requested)` is `true`, matching 05.1/05.8; T014's 65-relationship probe script (`notes/T014-check.php`) still passes in full, confirming the new casts didn't disturb any relationship key.
- Deviation: none
- Follow-up: 07.11's 409-to-flash-message rendering isn't wired into `bootstrap/app.php` yet — add it when the first web-facing status action lands. `TableStatusService` (not built yet) needs a distinct override method (reason required, writes to `audit_log`) for 5.6's admin override, separate from `ensureCanTransitionTo()`. 06.3 lists six more enumerated columns with no enum among 07.3's 11 — `role.role_name`, `reservation.cancelled_by`, `order_status_history.event_source`, `payment.adjustment_category`, `setting.value_type`, `historical_data_management.status`. Left as plain strings validated with `Rule::in([...])` in Form Requests; turning them into enums would go beyond 07.3's named list, so that's a decision for the developer, not something to do unasked.

### 2026-09-19 — Task 11 (T014) — done — Claude Code (models generated by developer via `make:model`, filled in and verified by Claude Code)
- Added / changed: `app/Models/Role.php`, `Staff.php`, `Customer.php`, `Setting.php`, `RestaurantTable.php`, `SlotCapacity.php`, `Visit.php`, `Reservation.php`, `MenuCategory.php`, `MenuItem.php`, `MenuItemSize.php`, `AddOnGroup.php`, `AddOnOption.php`, `Allergen.php`, `MenuItemAllergen.php`, `DietaryTag.php`, `MenuItemDietaryTag.php`, `Order.php`, `OrderItem.php`, `OrderStatusHistory.php`, `Payment.php`, `Refund.php`, `Feedback.php`, `AuditLog.php`, `HistoricalDataManagement.php`, `docs/PROGRESS.md`
- Notes: 25 models per 07.3, one per table, read against 06.2's ERD and 06.4's column details. Every model sets `$table`/`$primaryKey` explicitly rather than relying on Laravel's pluralisation guess. Four timestamp patterns used depending on what each table actually has: Laravel's default (both columns), `const UPDATED_AT = null` (created_at only: staff, customer, restaurant_table, visit, payment), `const CREATED_AT = null` (setting), and `public $timestamps = false` (everything else, where the DB fills its own datetime default). `Setting` uses a string key (`setting_key`) with `$incrementing = false`/`$keyType = 'string'`. `Feedback` uses `order_id` as its key (not auto-incrementing) since one feedback row is tied to one order (BR43). `OrderStatusHistory` has `$primaryKey = null` since its key is composite (order_id, status_seq) and Eloquent can't represent that; safe because 06.4.20 says the table is insert-only. `MenuItemAllergen`/`MenuItemDietaryTag` extend `Pivot`, generated with `make:model --pivot`. Every FK column got an explicit `belongsTo` with both keys spelled out (36 ERD relationships covered from the child side); parent-side `hasMany`/`hasOne`/`belongsToMany` added only where 07.4's services actually need to navigate that way — the "who did this" staff columns (reviewed_by, closed_by, etc.) are belongsTo-only. `restaurantTable()` used instead of `table()` to avoid colliding with Eloquent's own `$table` property. `$guarded` applied consistently: each model's own auto-increment PK, every column that is a state-machine status per 05.1–05.8 (`orders.status`/`payment_status`, `order_item.status`, `payment.status`, `refund.status`) enforcing hard rule 3 (status changes only through transition maps, not mass assignment), and `payment.succeeded_order_id` (a DB-generated column that MySQL/MariaDB reject writes to). `restaurant_table.status`/`reservation.status` also guarded for the same reason. Casts added for booleans, datetimes, `date` (booking_date), `array` (JSON columns, and `historical_data_management.record_data` which is `text` holding JSON), and `decimal:2` for every money/nutrition column (returns as string, avoids float rounding). Status enum casts deferred to T015. `Staff`/`Customer` extend `Authenticatable` (07.5 guards), set `$authPasswordName = 'password_hash'`, cast it `hashed`, and hide `password_hash`/`remember_token`. `RestaurantTable` hides `qr_token` (NFR04). Verified with a relationship-probe script (`notes/T014-check.php`, gitignored) that calls `Model::has('relation')->count()` for all 65 relationship methods against the real MariaDB schema — a wrong key name fails immediately as unknown column, rather than silently working until some future query. All 65 passed. Also verified: both guards resolve to the right model and `password_hash`; assigning a plain password on `Staff` produces a verifiable bcrypt hash; `new Order(['status' => 'paid', 'table_id' => 1])` drops `status` and keeps `table_id`, confirming the guard; `php artisan model:show Order` confirms `status`/`payment_status` show as not fillable and all 9 declared relationships resolve. Compared DB clock (`select now()`) against PHP's `now()` — both read `2026-09-19 11:52:29`, so no timezone mismatch between MariaDB's server clock and `APP_TIMEZONE=Australia/Melbourne`; nothing to flag. `php artisan test` green (1 passed).
- Deviation: none
- Follow-up: no factories yet — `HasFactory` is used on every model per Laravel convention, but no `database/factories/*.php` exist; added when tests need them. T015 adds the 11 backed enums and wires them into each model's `casts()`. T020/T021 need `MustVerifyEmail` decided for `Customer` (BR42 requires verified email before reservations) and will use the `password` credentials key against `password_hash` via `authPasswordName`. `notes/T014-check.php` is local-only (notes/ is gitignored), kept for reuse if models change later.

### 2026-09-19 — Task 10 (T013) — done — Claude Code
- Added / changed: `database/migrations/2026_09_19_111845_create_orders_table.php`, `111847_create_order_item_table.php`, `111848_create_order_status_history_table.php`, `111850_create_payment_table.php`, `111851_create_refund_table.php`, `111852_create_feedback_table.php`, `111854_create_audit_log_table.php`, `111855_create_historical_data_management_table.php`, `docs/PROGRESS.md`
- Notes: Direct read of 06.4.18–06.4.25; no schema invented (hard rule 1). Dependency order: orders → order_item → order_status_history → payment → refund → feedback → audit_log → historical_data_management. `order_item` uses a surrogate PK plus `UNIQUE (order_id, line_no)`, as 06.4.19 states. That settles the root brief's composite-vs-surrogate question, so nothing needed asking. Composite PK on `order_status_history (order_id, status_seq)`. `feedback.order_id` is both the PK and the FK (BR43). `bigIncrements` for `audit_log.log_id` / `historical_data_management.history_id`. `payment.succeeded_order_id` is a STORED generated column with a unique index (BR25). Its expression is written `CASE WHEN status = 'succeeded' THEN order_id END` rather than the dictionary's `IF(status='succeeded', order_id, NULL)`. The result is identical, but `IF()` doesn't exist in SQLite and tests run migrations on in-memory SQLite. The feedback CHECK (`food_rating`/`service_rating` 1–5, listed in 06.4.23) is added with a raw `ALTER TABLE`, because Blueprint has no `check()`. It's skipped on SQLite, which can't `ADD CONSTRAINT`. FKs with no stated delete rule use 06.1's RESTRICT default: `orders.visit_id/customer_id/taken_by_staff_id`, `payment.recorded_by_staff_id`, `refund` FKs other than `order_id`, `feedback.customer_id/replied_by_staff_id`, `audit_log.customer_id`. The single-column `INDEX (item_id)` on `order_item` and `INDEX (order_id)` on `refund` were not added explicitly, because InnoDB creates them for the FKs (`getIndexes()` confirms both). Verified after `migrate:fresh`: every column, index and FK delete rule matches, using `getColumns()`/`getIndexes()`/`getForeignKeys()`. In a rolled-back transaction, a second succeeded payment for the same order is rejected (duplicate key on `payment_succeeded_order_id_unique`), failed attempts alongside it are allowed (`succeeded_order_id` = NULL), `food_rating = 6` is rejected by `feedback_rating_check`, and 5/1 is accepted. All 28 migrations also run cleanly against in-memory SQLite (`DB_CONNECTION=sqlite DB_DATABASE=:memory: php artisan migrate:fresh`). `php artisan test` green (1 passed).
- Deviation: none in schema. The generated-column expression text differs from 06.4.21 (CASE vs IF), with identical behaviour. Consider updating 06.4.21's wording to the CASE form so the SDD and code match literally.
- Follow-up: local DB is MariaDB 10.4.32 (XAMPP), not MySQL 8 as 06.1 states. Everything so far works on it. `json` columns come out as `longtext` + `JSON_VALID` check on MariaDB, which is fine for Laravel `array` casts. Decide whether 06.1 should say "MySQL 8 / MariaDB 10.4+" or whether a real MySQL 8 is needed for submission. The feedback rating CHECK isn't enforced under SQLite tests, so the feedback Form Request must validate 1–5 itself. Service-layer rules noted in 06.4 but not DB-enforced: `feedback.customer_id` must equal `orders.customer_id`, `refund` totals ≤ succeeded payment (BR27), `refunded_qty` ≤ `quantity`, `order_status_history`/`audit_log` are insert-only, and unpaid orders are cancelled by the daily reset (BR26).

### 2026-09-19 — Task 9 (T012) — done — Claude Code
- Added / changed: `database/migrations/2026_09_19_110901_create_menu_category_table.php`, `110902_create_menu_item_table.php`, `110904_create_menu_item_size_table.php`, `110905_create_add_on_group_table.php`, `110907_create_add_on_option_table.php`, `110908_create_allergen_table.php`, `110910_create_menu_item_allergen_table.php`, `110911_create_dietary_tag_table.php`, `110913_create_menu_item_dietary_tag_table.php`, `docs/PROGRESS.md`
- Notes: Direct read of 06.4.7–06.4.15; no schema invented (hard rule 1). Migrations run in dependency order: category → item → size → add-on group → add-on option → allergen → item_allergen → dietary_tag → item_dietary_tag. Explicit-width methods throughout, including `smallIncrements` for the `smallint unsigned` `allergen_id`/`dietary_tag_id` PKs. Money columns (`price`, `sale_price`, `price_delta`) are `decimal(8,2)` per 06.1; nutrition columns use the dictionary's `decimal(7,2)`/`decimal(6,2)`. The two junction tables use composite primary keys (`item_id` + `allergen_id`/`dietary_tag_id`) as weak entities, per the dictionary's PF keys. `menu_category.parent_category_id` is a self-referencing FK (RESTRICT); the rule that a parent must itself be top level (A4) belongs in the service. The `daily_limit`/`sold_today` CHECK is left out on purpose, because 06.4.8 says it is not enforced (BR54). Value rules (`price > 0`, `sale_price < price`, `max_select >= min_select` BR16) belong in Form Requests. Every composite index from the dictionary was added. The single-column `INDEX (allergen_id)` and `INDEX (dietary_tag_id)` were not added explicitly, because InnoDB creates them for the FKs (`getIndexes()` confirms `menu_item_allergen_allergen_id_foreign` and `menu_item_dietary_tag_dietary_tag_id_foreign` exist). All delete rules are stated in the dictionary: CASCADE on every `item_id`/`group_id` FK, RESTRICT on `category_id`, `parent_category_id`, `allergen_id`, `dietary_tag_id`. After `migrate:fresh`, verified every column type, default, index and FK delete rule with `Schema::getColumns()`/`getIndexes()`/`getForeignKeys()`. `php artisan test` green (1 passed).
- Deviation: none
- Follow-up: no models yet (T014), no enum for `menu_item.destination` yet (T015), no seed data yet (T016). Option allergens must be a subset of the parent item's allergens (06.4.11). That is a service rule for the menu admin task.

### 2026-09-19 — Task 8 (T011) — done — Claude Code
- Added / changed: `database/migrations/2026_09_19_110227_create_restaurant_table_table.php`, `2026_09_19_110231_create_slot_capacity_table.php`, `2026_09_19_110235_create_reservation_table.php`, `2026_09_19_110240_create_visit_table.php`, `docs/PROGRESS.md`
- Notes: Direct read of 06.4.5, 06.4.6, 06.4.16, 06.4.17 against the approved data dictionary; no schema invented (hard rule 1). Migration order is dependency order, not the task list's reading order — `visit` has an FK into `reservation`, so it migrates last, not third. Explicit-width Blueprint methods throughout (`increments`, `unsignedInteger`, `unsignedTinyInteger`, `unsignedSmallInteger`), same reasoning as T010: the dictionary's declared widths would otherwise get silently widened by `id()`/`foreignId()`. Six FKs (`reservation`'s four staff FKs, `visit`'s two staff FKs) have no delete rule stated in 06.4.16/06.4.17; applied 06.1's stated default ("ON DELETE RESTRICT unless stated") rather than guessing, consistent with T010's `staff.role_id`. Skipped a redundant `INDEX (reservation_id)` on `visit` that 06.4.16 lists — InnoDB already creates that index for the FK constraint itself; added the three composite indexes (06.4.16, 06.4.17) that add a second column an FK-only index wouldn't cover. `seat_capacity > 0` (BR33) and BR-driven bounds on `party_size` are Form Request validation, not a SQL CHECK constraint — 06.4 doesn't ask for one. Verified column widths and FK delete rules with `Schema::getColumns()`/`Schema::getForeignKeys()` after `migrate:fresh` — `reservation`/`visit`'s `*_staff_id`/`customer_id`/`slot_id`/`table_id` columns came back `int(10) unsigned`, `seat_capacity`/`party_size`/`guest_count` came back `tinyint(3) unsigned`, and `visit_reservation_id_foreign` came back `on_delete: set null` with every other FK on both tables `restrict`. `php artisan test` green (1 passed).
- Deviation: none
- Follow-up: no Eloquent models yet (T014). No seed data yet (T016) — `slot_capacity` and `restaurant_table` are both seeded per 06.5. The "at most one open visit per table" rule (06.4.16) is a row-lock service constraint, not enforced at the DB layer — flagged for whichever service opens/closes visits. `restaurant_table.status` transitions to `reserved` only via the T-30 scheduler job (BR04) — not built yet.

### 2026-09-19 — Task 7 (T010) — done — Claude Code (migrations typed by developer, verified by Claude Code)
- Added / changed: `database/migrations/2026_09_19_104713_create_role_table.php`, `2026_09_19_104728_create_staff_table.php`, `2026_09_19_104737_create_customer_table.php`, `2026_09_19_104745_create_setting_table.php`, `tests/Feature/.gitkeep` (new), `docs/PROGRESS.md`
- Notes: Direct read of 06.4.1–06.4.4 against the approved data dictionary; no schema invented (hard rule 1). Explicit-width Blueprint methods (`tinyIncrements`, `increments`, `unsignedTinyInteger`, `unsignedInteger`) throughout instead of `id()`/`foreignId()`, which default to bigint unsigned and would have silently widened `role_id`/`staff_id`/`customer_id`/`updated_by_staff_id` past the dictionary's `tinyint unsigned`/`int unsigned`. No table uses `$table->timestamps()` — none of the four have the standard nullable created_at+updated_at pair; each timestamp column is declared individually as `dateTime()` (staff/customer `created_at` NOT NULL DEFAULT CURRENT_TIMESTAMP via `useCurrent()`; setting `updated_at` nullable only), matching the dictionary's explicit "datetime" type over MySQL's TIMESTAMP (session-timezone conversion, 2038 ceiling). `password_hash` kept as named in the dictionary, not Laravel's conventional `password`. Migration order (role → staff → customer → setting) satisfies both FKs (staff.role_id → role RESTRICT, setting.updated_by_staff_id → staff SET NULL) without forward references. After `migrate:fresh`, verified with `Schema::getColumns()`, `Schema::getIndexes()` and `Schema::getForeignKeys()` — not just that the migrations ran — that every width, default, nullability, unique index and FK matches 06.4.1–06.4.4 exactly (role_id came back `tinyint(3) unsigned`, staff_id/customer_id/updated_by_staff_id `int(10) unsigned`, is_active default `1`, value_type default `'string'`, created_at default `current_timestamp()`, both FKs present with the right `on_delete`). Also fixed a regression surfaced by `php artisan test` failing with "Test directory tests/Feature not found": T006 deleted the only file in `tests/Feature/` (`ExampleTest.php`), and Git doesn't track empty directories, so the directory itself silently dropped out of the checkout — `phpunit.xml` requires it to exist as a testsuite, so PHPUnit refused to run *anything*, not just Feature tests. Fixed with a tracked `.gitkeep` placeholder; `php artisan test` green again (1 passed — `tests/Unit/ExampleTest.php`).
- Deviation: none
- Follow-up: no Eloquent models yet (T014) — providers in `config/auth.php` (T002) already point at `App\Models\Customer`/`App\Models\Staff`, harmless until those classes exist. No seed data yet (T016) — `role` and `setting` are both seeded tables per 06.5 but sit empty until then. `staff.password_hash`/`customer.password_hash` being non-standard column names is a T020/T021 concern (Auth config `password` key), not this task's. `tests/Feature/.gitkeep` should be deleted the moment a real Feature test lands in T020 — it only exists to keep the directory alive.

### 2026-09-19 — Task 6 (T006) — done — Claude Code
- Added / changed: `public/css/style.css`, `public/css/dashboard.css`, `public/images/logo.svg`, `vite.config.js`, `routes/web.php`, `docs/PROGRESS.md`
- Removed: `resources/css/app.css`, `resources/views/welcome.blade.php`, `tests/Feature/ExampleTest.php`; the default `/` route
- Notes: `style.css`/`dashboard.css` copied from `docs/design/` as the two masters (NFR17, AGENTS rule 4). Only `logo.svg` copied to `public/images/` — the rest of the prototype's demo photos stay in `docs/design/…/images/` for T030 to choose from against real homepage content, not duplicated here. Pulled `.order-bar`/`.table-chip` out of temp-2's `customer-pages.css` into `style.css` (needed now by T005's order-bar partial); everything else in that file, plus all of `role-dashboard.css` and `tables-page.css`, stays out until the tasks that build those screens (T020 auth, kitchen/waitstaff station tasks, T050 tables). Found and fixed a real bug while reading `dashboard.css`: the prototype's sidebar active-state relied entirely on `body:has(#id:target)`, a single-page anchor trick that can never match our routed pages — `.nav-item.is-active` (what T005's Blade actually sets) had no base style at all, so the sidebar highlighted nothing; added the missing rule, deleted the dead `:has()` block. Also fixed: sidebar drawer breakpoint moved 768px → 900px to match 08.7 (merged into the existing 900px query block, confirmed the file's other, unrelated 900px block near the form styles was left untouched). Added `.nav-state:focus-visible ~ .shell .burger{outline:...}` so tabbing to the sidebar toggle is visible (08.7), matching `style.css`'s existing pattern for the public hamburger. Removed the Tailwind scaffold flagged as a follow-up since **T001**: `resources/css/app.css`, its `vite.config.js` entry, and the unrelated Instrument-Sans `bunny()` font loader. That required also removing `welcome.blade.php` (not an SDD screen; would have thrown on the missing Vite entry) and the `/` route pointing at it, and deleting `tests/Feature/ExampleTest.php` (asserted `GET / == 200`, no longer true and nothing Coolaroo-specific to replace it with — `tests/Unit/ExampleTest.php` is untouched). Verified: `npm run build` succeeds with a manifest containing only `app.js`; all five `/_preview/*` views render cleanly via `php artisan tinker` (`view(...)->render()`, sidestepping this sandbox's MySQL-less session driver — `GET` over HTTP 500s here only because `SESSION_DRIVER=database` needs a live MySQL connection this environment doesn't have running; unrelated to this task's changes); `php -l routes/web.php` clean; `php artisan test` green (1 passed — `tests/Unit/ExampleTest.php`, the only test left after removing `Feature/ExampleTest.php`).
- Deviation: none — CSS breakpoint fix aligns the code with 08.7, per AGENTS.md ("if code and the SDD disagree, fix both in the same change").
- Follow-up: `GET /` 404s until T030 builds the real homepage — expected, not a bug. T020 merges `customer-pages.css`'s `.auth-*` rules when the login/register/reset views are built. Station tasks (kitchen/waitstaff KDS, floor view) merge `role-dashboard.css` and set `body_class` on those pages (the `staff.blade.php` `@yield('body_class')` slot from T005 is already there waiting for it). T050 merges `tables-page.css`. NFR18 (W3C HTML/CSS validation) still needs a manual browser check against `/_preview/public` and `public/css/style.css` — not run yet, flagged for the developer to do by hand since the W3C validator can't reach `127.0.0.1`. Re-check against the real homepage once T030 lands. Confirm before running the app locally that MySQL (XAMPP) is actually started — this session's 500s on `/_preview/*` over HTTP were purely a "MySQL not running" local-environment issue, not a code defect.

### 2026-09-19 — Task 5 (T005) — done — Claude Code
- Added / changed: `resources/views/layouts/{public,customer,staff,admin}.blade.php`, `resources/views/layouts/partials/{head,site-header,site-footer,order-bar,nav-item,nav-admin,nav-staff,dashboard-topbar,dashboard-footer}.blade.php`, `resources/views/preview/{public,customer,customer-table,staff,admin}.blade.php`, `routes/web.php`, `docs/PROGRESS.md`
- Notes: `@extends`/`@yield` layouts per 07-architecture's folder layout. Public + admin shells rebuilt from `docs/design/{index,dashboard}.html`; customer (order-bar) and staff (sidebar) shells from `docs/design/colaroo html temp 2/`, which is untracked reference material, not copied in as-is. Admin sidebar = the 14 items in 08.5; staff sidebar = S22/S27/S30 (floor, reservations, kitchen/bar KDS). Nav links use fixed 08.1 paths via `url()` (no named routes exist yet); active state via `request()->is()` + `aria-current`. Echo (`@vite('resources/js/app.js')`) loaded on customer/staff/admin only, not public. 08.7 applied throughout: `lang="en-AU"`, no self-closing void tags, `aria-hidden` on decorative SVGs, labels on toggle inputs, `data-testid` on every interactive control. Dropped from the prototypes as out of SDD scope or fake controls: topbar search box and notification bell, footer newsletter signup, footer social links, sidebar shift note, nav count pills. Footer headings changed `<h4>` → `<h2 class="foot-h">` to fix heading-order (T006 must style `.foot-h`). Five local-only `/_preview/*` routes added behind `app()->isLocal()` to eyeball the shells before real pages/CSS exist; `php artisan route:list --path=_preview` confirms all 5 registered, `php artisan test` still green (2 passed).
- Deviation: none
- Follow-up: T006 needs `css/style.css`, `css/dashboard.css`, `images/logo.svg` to exist; must merge temp-2's `customer-pages.css`/`role-dashboard.css`/`tables-page.css` into the two allowed stylesheets (AGENTS rule 4, no extra CSS files); must add a `:focus-visible` outline for the dashboard sidebar burger (currently only `style.css`'s public hamburger has one — 08.7 "visible focus" is not yet met on staff/admin); must set the sidebar drawer breakpoint to 900 px per 08.7 (the prototype's own comment says 768 px); must restyle `.foot h4` → `.foot-h`; should remove the leftover `resources/css/app.css` Tailwind scaffold and Instrument Sans `bunny()` font from `vite.config.js`. Settings (T016+): footer venue/hours/phone and admin footer address are static prototype copy, not DB-driven yet — swap to `setting` values and add real social links then. T020/T021: logout forms and the staff profile menu are placeholders (a `<span>`, not a `<button>`) until those tasks land; T021 also wires the sidebar brand link to `role.landing_screen` instead of the current hardcoded `/staff/floor`. T022: staff/admin nav is unfiltered by role until then. T040: add allergen/dietary-tag paths to the Categories nav match once those routes exist. `/admin/reports` has no screen of its own in 08.1 (only `/admin/reports/{type}`) — T041 area's report task decides the index behaviour. Remove the `/_preview` routes and `resources/views/preview/` once real pages replace them — flagged so this doesn't quietly become permanent scaffolding.
- Flag: `docs/design/dashboard.html` opens with a comment "Theme Name: sparkAdmin / Source: ThemeWagon" — confirm `dashboard.css` is original work (or check that template's licence) before T006 imports it; nothing from that comment was carried into the Blade layouts.

### 2026-09-19 — Task 4 (T004) — done — Claude Code
- Added / changed: `config/services.php`, `.env`, `.env.example`, `docs/PROGRESS.md`
- Notes: Added `stripe` (`key`, `secret`) and `ai` (`base_url`, `model`, `api_key`, `timeout`) blocks per 07.9, with env var names taken from 10.3. `AI_BASE_URL=https://models.github.ai/inference` and `AI_MODEL=openai/gpt-4.1-mini` are set literally in both `.env` and `.env.example` — not secrets, and `config/services.php` carries the same values as fallback defaults. The 20-second GitHub Models timeout from 07.9 is hardcoded in the config rather than given an env var, since 10.3 names none. Deliberately did **not** install `stripe/stripe-php`: nothing calls it until T070, and installing early risks a repeat of T003's Guzzle resolution conflict. GitHub Models needs no package (OpenAI-compatible HTTP, called via Laravel's `Http` client in T120).
- Deviation: none
- Follow-up: `STRIPE_KEY`, `STRIPE_SECRET` and `AI_API_KEY` are intentionally blank — real credentials (Stripe **test mode** key pair, GitHub PAT with Models access) must be added to the local `.env` before T070/T120 can hit the live APIs. Never committed (AGENTS.md hard rule 9). `stripe/stripe-php` gets installed in T070.

### 2026-09-19 — Task 3 (T003) — done — Claude Code
- Added / changed: `composer.json`, `composer.lock`, `package.json`, `package-lock.json`, `.env`, `.env.example`, `bootstrap/app.php`, `resources/js/app.js`, `routes/channels.php` (new), `config/broadcasting.php` (new), `config/reverb.php` (new), `resources/js/echo.js` (new), `docs/PROGRESS.md`
- Notes: `composer require laravel/reverb -W` — needed `-W` because every Reverb release requires `guzzlehttp/psr7 ^2.6` while `laravel/framework` had resolved `guzzlehttp/guzzle` to its 8.x line (requiring `psr7 ^3.1`); Composer re-resolved the graph and downgraded guzzle to 7.15.5. `php artisan install:broadcasting` (chose Reverb) scaffolded `config/broadcasting.php`, `routes/channels.php`, the `channels:` wiring in `bootstrap/app.php`, `laravel-echo`/`pusher-js`, and `resources/js/echo.js`, but left `REVERB_*` env vars unset and `config/reverb.php` unpublished. `php artisan reverb:install` then crashed (`Pusher\Pusher::__construct(): $auth_key ... null given`) because `BROADCAST_CONNECTION=reverb` was already set with no key yet — fixed by adding the `REVERB_*`/`VITE_REVERB_*` block to `.env` by hand first (values read from the installer's own source so they'd match its real defaults). Re-running it hung at its last interactive confirm (Windows/PowerShell + Laravel Prompts rendering issue, harmless to Ctrl+C) and — separately — duplicated the `REVERB_*` block instead of skipping it, because its "already present" check looks for `\r\n` + the var name but `.env` uses plain `\n` line endings; removed the duplicate by hand. Also found and fixed the same duplicate-key pattern on `BROADCAST_CONNECTION` itself (`install:broadcasting` appended a second `BROADCAST_CONNECTION=reverb` line near the end instead of updating the original `=log` line in place) — consolidated to one line. Mirrored all new `.env` keys into `.env.example` with placeholder values. Replaced the installer's default `routes/channels.php` example (`App.Models.User.{id}`, referencing the model deleted in T002) with a placeholder comment — real channels land in T110. Queue/cache/session drivers needed no changes; already `database` since T001, matching 07.2's local row.
- Deviation: none
- Follow-up: none. `config/reverb.php` and `config/broadcasting.php` are framework defaults, untouched beyond what the installer generated.

### 2026-09-19 — Task 2 (T002) — done — Claude Code
- Added / changed: `config/auth.php`, `database/seeders/DatabaseSeeder.php`, `.obsidian/` untracked + gitignored, `.gitignore`, `AGENTS.md`
- Removed: `app/Models/User.php`, `database/factories/UserFactory.php`, `database/migrations/0001_01_01_000000_create_users_table.php`
- Added (new file): `database/migrations/0001_01_01_000000_create_password_reset_tokens_and_sessions_tables.php` — same file, renamed and trimmed to drop the `users` table while keeping `password_reset_tokens`/`sessions` (06.1: those are framework infra, not design entities, and stay)
- Notes: Two guards (`customer`, `staff`) per 07.5, each with its own Eloquent provider and password-reset broker (FR05 applies to all users). Providers point at `App\Models\Customer`/`App\Models\Staff`, which don't exist until T014 — harmless, since `::class` never autoloads. `php artisan migrate:fresh` rebuilds `cache`/`cache_locks`/`jobs`/`job_batches`/`failed_jobs`/`password_reset_tokens`/`sessions`/`migrations` with no `users` table; `php artisan test` still green. Also untracked an accidentally-committed `.obsidian/` folder (unrelated editor config) and added it to `.gitignore`. `AGENTS.md` updated with two new standing rules: task guides go in `notes/<Txxx>-guide.md`, and terminal commands belong inline within each guide step rather than a separate list at the end.
- Deviation: none
- Follow-up: both new password-reset brokers (`customers`, `staff`) share the single `password_reset_tokens` table, keyed only by email with no guard/type column (06.1 treats it as shared framework infra) — a customer and staff member with the same email would share a reset-token row. Accepted as-is per the SDD; not revisited unless it becomes a real problem. T010 creates the actual `customer`/`staff` tables; T014 creates the models the guards already point to.

### 2026-09-18 — Task 1 (T001) — done — Claude Code
- Added / changed: `.env`, `.env.example`, `config/app.php`, `package.json`, `package-lock.json`, `vite.config.js`, `resources/css/app.css`, `.gitignore`
- Notes: DB switched from SQLite to MySQL (`coolaroo`, 07.2). `APP_TIMEZONE=Australia/Melbourne` set in `.env` and in `config/app.php`'s `'timezone'` key (NFR13) — Laravel 11+'s slimmed `config/app.php` hardcodes `'timezone' => 'UTC'` instead of reading `.env`, so both had to change for the setting to actually take effect. `APP_NAME` set to "Coolaroo RMS". Removed the Tailwind 4 scaffold (`package.json`, `vite.config.js`, `resources/css/app.css`) per AGENTS.md rule 4 (no CSS frameworks). Created the `develop` branch. Added `/notes/` to `.gitignore` for local task-guide files (kept outside the SDD-tracked `docs/` folder).
- Deviation: none
- Follow-up: `resources/css/app.css` still has two leftover `@source '...'` directives from the removed Tailwind setup — inert without the Tailwind plugin, but should be deleted when T006 wires up the real `public/css/style.css` / `dashboard.css` pipeline. T002 removes the default `users`/`cache`/`jobs` tables created by today's `migrate`.
